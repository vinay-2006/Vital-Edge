{
  "name": "vital-edge-local-tools",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev:imaging": "npm --prefix experimental-imaging run dev",
    "build:imaging": "npm --prefix experimental-imaging run build",
    "typecheck:imaging": "npm --prefix experimental-imaging run typecheck"
  }
}
